import { ContraseniaPipe } from './contrasenia.pipe';

describe('ContraseniaPipe', () => {
  it('create an instance', () => {
    const pipe = new ContraseniaPipe();
    expect(pipe).toBeTruthy();
  });
});
